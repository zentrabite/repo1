import PlaceholderPage from "@/components/dashboard/pages/PlaceholderPage";

export const metadata = { title: "Customers — ZentraBite" };

export default function CustomersPage() {
  return (
    <PlaceholderPage
      title="Customers"
      description="Full CRM — search, segment, and view every customer's profile and order history."
      accentColor="#00B67A"
    />
  );
}
