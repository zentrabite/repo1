import PlaceholderPage from "@/components/dashboard/pages/PlaceholderPage";

export const metadata = { title: "Menu — ZentraBite" };

export default function MenuPage() {
  return (
    <PlaceholderPage
      title="Menu"
      description="Drag-to-reorder categories, toggle availability, upload images, and manage modifiers."
      accentColor="#00B67A"
    />
  );
}
