import PlaceholderPage from "@/components/dashboard/pages/PlaceholderPage";

export const metadata = { title: "Orders — ZentraBite" };

export default function OrdersPage() {
  return (
    <PlaceholderPage
      title="Orders"
      description="Real-time Kanban board (New → Preparing → Ready → Delivered) plus historical list view."
      accentColor="#FF6B35"
    />
  );
}
